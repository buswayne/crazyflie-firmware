CONFIG_PLATFORM_CF21BL=y

CONFIG_ESTIMATOR_AUTO_SELECT=y
CONFIG_CONTROLLER_SAFETY_FILTER=y
CONFIG_MOTORS_REQUIRE_ARMING=y
CONFIG_MOTORS_ESC_PROTOCOL_DSHOT=y
# Reverted to stock Kconfig defaults (idleThrust=7000, RPM window
# 1000-3500) -- lowering idleThrust to fix the "small liftoff at arm"
# cosmetic issue broke arming outright (motors spin then get stopped by the
# RPM-at-arming timeout, since the window was re-guessed twice without real
# telemetry and never matched). Known-working state restored; the liftoff
# issue is deferred until we have real motor.m1_rpm..m4_rpm log values to
# calibrate idleThrust and the RPM window together instead of guessing.
